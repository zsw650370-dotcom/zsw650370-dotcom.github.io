name: Publish legal library
on:
  push:
    branches: [main]
    paths:
      - laws-overlay-3.tar.b64
      - .github/workflows/deploy.yml
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: false
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - run: mkdir public && tar -xzf site.tar.gz -C public
      - name: Apply complete laws overlay
        run: cat laws-overlay.tar.b64 laws-overlay-2.tar.b64 laws-overlay-3.tar.b64 | base64 --decode | tar -xz -C public
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: public
      - uses: actions/deploy-pages@v4
        id: deployment
